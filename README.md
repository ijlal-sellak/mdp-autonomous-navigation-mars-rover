# mdp-autonomous-navigation-mars-rover
This project proposes an autonomous Mars rover navigation system modeled as a Markov Decision Process (MDP). The environment is discretized into a grid with obstacles and stochastic transitions. An optimal navigation policy is computed using value iteration, enabling the rover to reach its target while minimizing navigation cost under uncertainty.
